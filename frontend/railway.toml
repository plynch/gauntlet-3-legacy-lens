[build]
builder = "NIXPACKS"
buildCommand = "if [ -f package.json ]; then npm ci && npm run build; else cd frontend && npm ci && npm run build; fi && npm install -g serve"

[deploy]
startCommand = "if [ -d dist ]; then serve -s dist -l $PORT; else serve -s frontend/dist -l $PORT; fi"
